interface:
  display_name: "Grilling"
  short_description: "Continue a decision interview with native questions"
  default_prompt: "Use $grilling to interview me in dependency-aware rounds and keep going after each answer until we confirm shared understanding."
policy:
  allow_implicit_invocation: true
