interface:
  display_name: "What"
  short_description: "Re-explain the last update with context"
  default_prompt: "Use $what to re-explain the last update with context."
policy:
  allow_implicit_invocation: false
